#ifndef _AVM_BYTECODE_INTERPRETER_H_
#define _AVM_BYTECODE_INTERPRETER_H_

#include "runtime/vm_thread.h"

namespace avm {
    class Interpreter{
    public:
        Interpreter();
        virtual ~Interpreter();
    public:
        void execute(VmThread* thread);
        void invoke(const ConstantPool* runtimeConstantPool, const Instruction* instruction, Frame* frame);
    public:
    };

    void invoke_nop             (const ConstantPool* runtimeConstantPool, const Instruction* instruction, Frame* frame); 
    void invoke_aconst_null     (const ConstantPool* runtimeConstantPool, const Instruction* instruction, Frame* frame); 
    void invoke_iconst_m1       (const ConstantPool* runtimeConstantPool, const Instruction* instruction, Frame* frame); 
    void invoke_iconst_0        (const ConstantPool* runtimeConstantPool, const Instruction* instruction, Frame* frame); 
    void invoke_iconst_1        (const ConstantPool* runtimeConstantPool, const Instruction* instruction, Frame* frame); 
    void invoke_iconst_2        (const ConstantPool* runtimeConstantPool, const Instruction* instruction, Frame* frame); 
    void invoke_iconst_3        (const ConstantPool* runtimeConstantPool, const Instruction* instruction, Frame* frame); 
    void invoke_iconst_4        (const ConstantPool* runtimeConstantPool, const Instruction* instruction, Frame* frame); 
    void invoke_iconst_5        (const ConstantPool* runtimeConstantPool, const Instruction* instruction, Frame* frame); 
    void invoke_lconst_0        (const ConstantPool* runtimeConstantPool, const Instruction* instruction, Frame* frame); 
    void invoke_lconst_1        (const ConstantPool* runtimeConstantPool, const Instruction* instruction, Frame* frame); 
    void invoke_fconst_0        (const ConstantPool* runtimeConstantPool, const Instruction* instruction, Frame* frame); 
    void invoke_fconst_1        (const ConstantPool* runtimeConstantPool, const Instruction* instruction, Frame* frame); 
    void invoke_fconst_2        (const ConstantPool* runtimeConstantPool, const Instruction* instruction, Frame* frame); 
    void invoke_dconst_0        (const ConstantPool* runtimeConstantPool, const Instruction* instruction, Frame* frame); 
    void invoke_dconst_1        (const ConstantPool* runtimeConstantPool, const Instruction* instruction, Frame* frame); 
    void invoke_bipush          (const ConstantPool* runtimeConstantPool, const Instruction* instruction, Frame* frame); 
    void invoke_sipush          (const ConstantPool* runtimeConstantPool, const Instruction* instruction, Frame* frame); 
    void invoke_ldc             (const ConstantPool* runtimeConstantPool, const Instruction* instruction, Frame* frame); 
    void invoke_ldc_w           (const ConstantPool* runtimeConstantPool, const Instruction* instruction, Frame* frame); 
    void invoke_ldc2_w          (const ConstantPool* runtimeConstantPool, const Instruction* instruction, Frame* frame); 
    void invoke_iload           (const ConstantPool* runtimeConstantPool, const Instruction* instruction, Frame* frame); 
    void invoke_lload           (const ConstantPool* runtimeConstantPool, const Instruction* instruction, Frame* frame); 
    void invoke_fload           (const ConstantPool* runtimeConstantPool, const Instruction* instruction, Frame* frame); 
    void invoke_dload           (const ConstantPool* runtimeConstantPool, const Instruction* instruction, Frame* frame); 
    void invoke_aload           (const ConstantPool* runtimeConstantPool, const Instruction* instruction, Frame* frame); 
    void invoke_iload_0         (const ConstantPool* runtimeConstantPool, const Instruction* instruction, Frame* frame); 
    void invoke_iload_1         (const ConstantPool* runtimeConstantPool, const Instruction* instruction, Frame* frame); 
    void invoke_iload_2         (const ConstantPool* runtimeConstantPool, const Instruction* instruction, Frame* frame); 
    void invoke_iload_3         (const ConstantPool* runtimeConstantPool, const Instruction* instruction, Frame* frame); 
    void invoke_lload_0         (const ConstantPool* runtimeConstantPool, const Instruction* instruction, Frame* frame); 
    void invoke_lload_1         (const ConstantPool* runtimeConstantPool, const Instruction* instruction, Frame* frame); 
    void invoke_lload_2         (const ConstantPool* runtimeConstantPool, const Instruction* instruction, Frame* frame); 
    void invoke_lload_3         (const ConstantPool* runtimeConstantPool, const Instruction* instruction, Frame* frame); 
    void invoke_fload_0         (const ConstantPool* runtimeConstantPool, const Instruction* instruction, Frame* frame); 
    void invoke_fload_1         (const ConstantPool* runtimeConstantPool, const Instruction* instruction, Frame* frame); 
    void invoke_fload_2         (const ConstantPool* runtimeConstantPool, const Instruction* instruction, Frame* frame); 
    void invoke_fload_3         (const ConstantPool* runtimeConstantPool, const Instruction* instruction, Frame* frame); 
    void invoke_dload_0         (const ConstantPool* runtimeConstantPool, const Instruction* instruction, Frame* frame); 
    void invoke_dload_1         (const ConstantPool* runtimeConstantPool, const Instruction* instruction, Frame* frame); 
    void invoke_dload_2         (const ConstantPool* runtimeConstantPool, const Instruction* instruction, Frame* frame); 
    void invoke_dload_3         (const ConstantPool* runtimeConstantPool, const Instruction* instruction, Frame* frame); 
    void invoke_aload_0         (const ConstantPool* runtimeConstantPool, const Instruction* instruction, Frame* frame); 
    void invoke_aload_1         (const ConstantPool* runtimeConstantPool, const Instruction* instruction, Frame* frame); 
    void invoke_aload_2         (const ConstantPool* runtimeConstantPool, const Instruction* instruction, Frame* frame); 
    void invoke_aload_3         (const ConstantPool* runtimeConstantPool, const Instruction* instruction, Frame* frame); 
    void invoke_iaload          (const ConstantPool* runtimeConstantPool, const Instruction* instruction, Frame* frame); 
    void invoke_laload          (const ConstantPool* runtimeConstantPool, const Instruction* instruction, Frame* frame); 
    void invoke_faload          (const ConstantPool* runtimeConstantPool, const Instruction* instruction, Frame* frame); 
    void invoke_daload          (const ConstantPool* runtimeConstantPool, const Instruction* instruction, Frame* frame); 
    void invoke_aaload          (const ConstantPool* runtimeConstantPool, const Instruction* instruction, Frame* frame); 
    void invoke_baload          (const ConstantPool* runtimeConstantPool, const Instruction* instruction, Frame* frame); 
    void invoke_caload          (const ConstantPool* runtimeConstantPool, const Instruction* instruction, Frame* frame); 
    void invoke_saload          (const ConstantPool* runtimeConstantPool, const Instruction* instruction, Frame* frame); 
    void invoke_istore          (const ConstantPool* runtimeConstantPool, const Instruction* instruction, Frame* frame); 
    void invoke_lstore          (const ConstantPool* runtimeConstantPool, const Instruction* instruction, Frame* frame); 
    void invoke_fstore          (const ConstantPool* runtimeConstantPool, const Instruction* instruction, Frame* frame); 
    void invoke_dstore          (const ConstantPool* runtimeConstantPool, const Instruction* instruction, Frame* frame); 
    void invoke_astore          (const ConstantPool* runtimeConstantPool, const Instruction* instruction, Frame* frame); 
    void invoke_istore_0        (const ConstantPool* runtimeConstantPool, const Instruction* instruction, Frame* frame); 
    void invoke_istore_1        (const ConstantPool* runtimeConstantPool, const Instruction* instruction, Frame* frame); 
    void invoke_istore_2        (const ConstantPool* runtimeConstantPool, const Instruction* instruction, Frame* frame); 
    void invoke_istore_3        (const ConstantPool* runtimeConstantPool, const Instruction* instruction, Frame* frame); 
    void invoke_lstore_0        (const ConstantPool* runtimeConstantPool, const Instruction* instruction, Frame* frame); 
    void invoke_lstore_1        (const ConstantPool* runtimeConstantPool, const Instruction* instruction, Frame* frame); 
    void invoke_lstore_2        (const ConstantPool* runtimeConstantPool, const Instruction* instruction, Frame* frame); 
    void invoke_lstore_3        (const ConstantPool* runtimeConstantPool, const Instruction* instruction, Frame* frame); 
    void invoke_fstore_0        (const ConstantPool* runtimeConstantPool, const Instruction* instruction, Frame* frame); 
    void invoke_fstore_1        (const ConstantPool* runtimeConstantPool, const Instruction* instruction, Frame* frame); 
    void invoke_fstore_2        (const ConstantPool* runtimeConstantPool, const Instruction* instruction, Frame* frame); 
    void invoke_fstore_3        (const ConstantPool* runtimeConstantPool, const Instruction* instruction, Frame* frame); 
    void invoke_dstore_0        (const ConstantPool* runtimeConstantPool, const Instruction* instruction, Frame* frame); 
    void invoke_dstore_1        (const ConstantPool* runtimeConstantPool, const Instruction* instruction, Frame* frame); 
    void invoke_dstore_2        (const ConstantPool* runtimeConstantPool, const Instruction* instruction, Frame* frame); 
    void invoke_dstore_3        (const ConstantPool* runtimeConstantPool, const Instruction* instruction, Frame* frame); 
    void invoke_astore_0        (const ConstantPool* runtimeConstantPool, const Instruction* instruction, Frame* frame); 
    void invoke_astore_1        (const ConstantPool* runtimeConstantPool, const Instruction* instruction, Frame* frame); 
    void invoke_astore_2        (const ConstantPool* runtimeConstantPool, const Instruction* instruction, Frame* frame); 
    void invoke_astore_3        (const ConstantPool* runtimeConstantPool, const Instruction* instruction, Frame* frame); 
    void invoke_iastore         (const ConstantPool* runtimeConstantPool, const Instruction* instruction, Frame* frame); 
    void invoke_lastore         (const ConstantPool* runtimeConstantPool, const Instruction* instruction, Frame* frame); 
    void invoke_fastore         (const ConstantPool* runtimeConstantPool, const Instruction* instruction, Frame* frame); 
    void invoke_dastore         (const ConstantPool* runtimeConstantPool, const Instruction* instruction, Frame* frame); 
    void invoke_aastore         (const ConstantPool* runtimeConstantPool, const Instruction* instruction, Frame* frame); 
    void invoke_bastore         (const ConstantPool* runtimeConstantPool, const Instruction* instruction, Frame* frame); 
    void invoke_castore         (const ConstantPool* runtimeConstantPool, const Instruction* instruction, Frame* frame); 
    void invoke_sastore         (const ConstantPool* runtimeConstantPool, const Instruction* instruction, Frame* frame); 
    void invoke_pop             (const ConstantPool* runtimeConstantPool, const Instruction* instruction, Frame* frame); 
    void invoke_pop2            (const ConstantPool* runtimeConstantPool, const Instruction* instruction, Frame* frame); 
    void invoke_dup             (const ConstantPool* runtimeConstantPool, const Instruction* instruction, Frame* frame); 
    void invoke_dup_x1          (const ConstantPool* runtimeConstantPool, const Instruction* instruction, Frame* frame); 
    void invoke_dup_x2          (const ConstantPool* runtimeConstantPool, const Instruction* instruction, Frame* frame); 
    void invoke_dup2            (const ConstantPool* runtimeConstantPool, const Instruction* instruction, Frame* frame); 
    void invoke_dup2_x1         (const ConstantPool* runtimeConstantPool, const Instruction* instruction, Frame* frame); 
    void invoke_dup2_x2         (const ConstantPool* runtimeConstantPool, const Instruction* instruction, Frame* frame); 
    void invoke_swap            (const ConstantPool* runtimeConstantPool, const Instruction* instruction, Frame* frame); 
    void invoke_iadd            (const ConstantPool* runtimeConstantPool, const Instruction* instruction, Frame* frame); 
    void invoke_ladd            (const ConstantPool* runtimeConstantPool, const Instruction* instruction, Frame* frame); 
    void invoke_fadd            (const ConstantPool* runtimeConstantPool, const Instruction* instruction, Frame* frame); 
    void invoke_dadd            (const ConstantPool* runtimeConstantPool, const Instruction* instruction, Frame* frame); 
    void invoke_isub            (const ConstantPool* runtimeConstantPool, const Instruction* instruction, Frame* frame); 
    void invoke_lsub            (const ConstantPool* runtimeConstantPool, const Instruction* instruction, Frame* frame); 
    void invoke_fsub            (const ConstantPool* runtimeConstantPool, const Instruction* instruction, Frame* frame); 
    void invoke_dsub            (const ConstantPool* runtimeConstantPool, const Instruction* instruction, Frame* frame); 
    void invoke_imul            (const ConstantPool* runtimeConstantPool, const Instruction* instruction, Frame* frame); 
    void invoke_lmul            (const ConstantPool* runtimeConstantPool, const Instruction* instruction, Frame* frame); 
    void invoke_fmul            (const ConstantPool* runtimeConstantPool, const Instruction* instruction, Frame* frame); 
    void invoke_dmul            (const ConstantPool* runtimeConstantPool, const Instruction* instruction, Frame* frame); 
    void invoke_idiv            (const ConstantPool* runtimeConstantPool, const Instruction* instruction, Frame* frame); 
    void invoke_ldiv            (const ConstantPool* runtimeConstantPool, const Instruction* instruction, Frame* frame); 
    void invoke_fdiv            (const ConstantPool* runtimeConstantPool, const Instruction* instruction, Frame* frame); 
    void invoke_ddiv            (const ConstantPool* runtimeConstantPool, const Instruction* instruction, Frame* frame); 
    void invoke_irem            (const ConstantPool* runtimeConstantPool, const Instruction* instruction, Frame* frame); 
    void invoke_lrem            (const ConstantPool* runtimeConstantPool, const Instruction* instruction, Frame* frame); 
    void invoke_frem            (const ConstantPool* runtimeConstantPool, const Instruction* instruction, Frame* frame); 
    void invoke_drem            (const ConstantPool* runtimeConstantPool, const Instruction* instruction, Frame* frame); 
    void invoke_ineg            (const ConstantPool* runtimeConstantPool, const Instruction* instruction, Frame* frame); 
    void invoke_lneg            (const ConstantPool* runtimeConstantPool, const Instruction* instruction, Frame* frame); 
    void invoke_fneg            (const ConstantPool* runtimeConstantPool, const Instruction* instruction, Frame* frame); 
    void invoke_dneg            (const ConstantPool* runtimeConstantPool, const Instruction* instruction, Frame* frame); 
    void invoke_ishl            (const ConstantPool* runtimeConstantPool, const Instruction* instruction, Frame* frame); 
    void invoke_lshl            (const ConstantPool* runtimeConstantPool, const Instruction* instruction, Frame* frame); 
    void invoke_ishr            (const ConstantPool* runtimeConstantPool, const Instruction* instruction, Frame* frame); 
    void invoke_lshr            (const ConstantPool* runtimeConstantPool, const Instruction* instruction, Frame* frame); 
    void invoke_iushr           (const ConstantPool* runtimeConstantPool, const Instruction* instruction, Frame* frame); 
    void invoke_lushr           (const ConstantPool* runtimeConstantPool, const Instruction* instruction, Frame* frame); 
    void invoke_iand            (const ConstantPool* runtimeConstantPool, const Instruction* instruction, Frame* frame); 
    void invoke_land            (const ConstantPool* runtimeConstantPool, const Instruction* instruction, Frame* frame); 
    void invoke_ior             (const ConstantPool* runtimeConstantPool, const Instruction* instruction, Frame* frame); 
    void invoke_lor             (const ConstantPool* runtimeConstantPool, const Instruction* instruction, Frame* frame); 
    void invoke_ixor            (const ConstantPool* runtimeConstantPool, const Instruction* instruction, Frame* frame); 
    void invoke_lxor            (const ConstantPool* runtimeConstantPool, const Instruction* instruction, Frame* frame); 
    void invoke_iinc            (const ConstantPool* runtimeConstantPool, const Instruction* instruction, Frame* frame); 
    void invoke_i2l             (const ConstantPool* runtimeConstantPool, const Instruction* instruction, Frame* frame); 
    void invoke_i2f             (const ConstantPool* runtimeConstantPool, const Instruction* instruction, Frame* frame); 
    void invoke_i2d             (const ConstantPool* runtimeConstantPool, const Instruction* instruction, Frame* frame); 
    void invoke_l2i             (const ConstantPool* runtimeConstantPool, const Instruction* instruction, Frame* frame); 
    void invoke_l2f             (const ConstantPool* runtimeConstantPool, const Instruction* instruction, Frame* frame); 
    void invoke_l2d             (const ConstantPool* runtimeConstantPool, const Instruction* instruction, Frame* frame); 
    void invoke_f2i             (const ConstantPool* runtimeConstantPool, const Instruction* instruction, Frame* frame); 
    void invoke_f2l             (const ConstantPool* runtimeConstantPool, const Instruction* instruction, Frame* frame); 
    void invoke_f2d             (const ConstantPool* runtimeConstantPool, const Instruction* instruction, Frame* frame); 
    void invoke_d2i             (const ConstantPool* runtimeConstantPool, const Instruction* instruction, Frame* frame); 
    void invoke_d2l             (const ConstantPool* runtimeConstantPool, const Instruction* instruction, Frame* frame); 
    void invoke_d2f             (const ConstantPool* runtimeConstantPool, const Instruction* instruction, Frame* frame); 
    void invoke_i2b             (const ConstantPool* runtimeConstantPool, const Instruction* instruction, Frame* frame); 
    void invoke_i2c             (const ConstantPool* runtimeConstantPool, const Instruction* instruction, Frame* frame); 
    void invoke_i2s             (const ConstantPool* runtimeConstantPool, const Instruction* instruction, Frame* frame); 
    void invoke_lcmp            (const ConstantPool* runtimeConstantPool, const Instruction* instruction, Frame* frame); 
    void invoke_fcmpl           (const ConstantPool* runtimeConstantPool, const Instruction* instruction, Frame* frame); 
    void invoke_fcmpg           (const ConstantPool* runtimeConstantPool, const Instruction* instruction, Frame* frame); 
    void invoke_dcmpl           (const ConstantPool* runtimeConstantPool, const Instruction* instruction, Frame* frame); 
    void invoke_dcmpg           (const ConstantPool* runtimeConstantPool, const Instruction* instruction, Frame* frame); 
    void invoke_ifeq            (const ConstantPool* runtimeConstantPool, const Instruction* instruction, Frame* frame); 
    void invoke_ifne            (const ConstantPool* runtimeConstantPool, const Instruction* instruction, Frame* frame); 
    void invoke_iflt            (const ConstantPool* runtimeConstantPool, const Instruction* instruction, Frame* frame); 
    void invoke_ifge            (const ConstantPool* runtimeConstantPool, const Instruction* instruction, Frame* frame); 
    void invoke_ifgt            (const ConstantPool* runtimeConstantPool, const Instruction* instruction, Frame* frame); 
    void invoke_ifle            (const ConstantPool* runtimeConstantPool, const Instruction* instruction, Frame* frame); 
    void invoke_if_icmpeq       (const ConstantPool* runtimeConstantPool, const Instruction* instruction, Frame* frame); 
    void invoke_if_icmpne       (const ConstantPool* runtimeConstantPool, const Instruction* instruction, Frame* frame); 
    void invoke_if_icmplt       (const ConstantPool* runtimeConstantPool, const Instruction* instruction, Frame* frame); 
    void invoke_if_icmpge       (const ConstantPool* runtimeConstantPool, const Instruction* instruction, Frame* frame); 
    void invoke_if_icmpgt       (const ConstantPool* runtimeConstantPool, const Instruction* instruction, Frame* frame); 
    void invoke_if_icmple       (const ConstantPool* runtimeConstantPool, const Instruction* instruction, Frame* frame); 
    void invoke_if_acmpeq       (const ConstantPool* runtimeConstantPool, const Instruction* instruction, Frame* frame); 
    void invoke_if_acmpne       (const ConstantPool* runtimeConstantPool, const Instruction* instruction, Frame* frame); 
    void invoke_goto            (const ConstantPool* runtimeConstantPool, const Instruction* instruction, Frame* frame); 
    void invoke_jsr             (const ConstantPool* runtimeConstantPool, const Instruction* instruction, Frame* frame); 
    void invoke_ret             (const ConstantPool* runtimeConstantPool, const Instruction* instruction, Frame* frame); 
    void invoke_tableswitch     (const ConstantPool* runtimeConstantPool, const Instruction* instruction, Frame* frame); 
    void invoke_lookupswitch    (const ConstantPool* runtimeConstantPool, const Instruction* instruction, Frame* frame); 
    void invoke_ireturn         (const ConstantPool* runtimeConstantPool, const Instruction* instruction, Frame* frame); 
    void invoke_lreturn         (const ConstantPool* runtimeConstantPool, const Instruction* instruction, Frame* frame); 
    void invoke_freturn         (const ConstantPool* runtimeConstantPool, const Instruction* instruction, Frame* frame); 
    void invoke_dreturn         (const ConstantPool* runtimeConstantPool, const Instruction* instruction, Frame* frame); 
    void invoke_areturn         (const ConstantPool* runtimeConstantPool, const Instruction* instruction, Frame* frame); 
    void invoke_return          (const ConstantPool* runtimeConstantPool, const Instruction* instruction, Frame* frame); 
    void invoke_getstatic       (const ConstantPool* runtimeConstantPool, const Instruction* instruction, Frame* frame); 
    void invoke_putstatic       (const ConstantPool* runtimeConstantPool, const Instruction* instruction, Frame* frame); 
    void invoke_getfield        (const ConstantPool* runtimeConstantPool, const Instruction* instruction, Frame* frame); 
    void invoke_putfield        (const ConstantPool* runtimeConstantPool, const Instruction* instruction, Frame* frame); 
    void invoke_invokevirtual   (const ConstantPool* runtimeConstantPool, const Instruction* instruction, Frame* frame); 
    void invoke_invokespecial   (const ConstantPool* runtimeConstantPool, const Instruction* instruction, Frame* frame); 
    void invoke_invokestatic    (const ConstantPool* runtimeConstantPool, const Instruction* instruction, Frame* frame); 
    void invoke_invokeinterface (const ConstantPool* runtimeConstantPool, const Instruction* instruction, Frame* frame); 
    void invoke_invokedynamic   (const ConstantPool* runtimeConstantPool, const Instruction* instruction, Frame* frame); 
    void invoke_new             (const ConstantPool* runtimeConstantPool, const Instruction* instruction, Frame* frame); 
    void invoke_newarray        (const ConstantPool* runtimeConstantPool, const Instruction* instruction, Frame* frame); 
    void invoke_anewarray       (const ConstantPool* runtimeConstantPool, const Instruction* instruction, Frame* frame); 
    void invoke_arraylength     (const ConstantPool* runtimeConstantPool, const Instruction* instruction, Frame* frame); 
    void invoke_athrow          (const ConstantPool* runtimeConstantPool, const Instruction* instruction, Frame* frame); 
    void invoke_checkcast       (const ConstantPool* runtimeConstantPool, const Instruction* instruction, Frame* frame); 
    void invoke_instanceof      (const ConstantPool* runtimeConstantPool, const Instruction* instruction, Frame* frame); 
    void invoke_monitorenter    (const ConstantPool* runtimeConstantPool, const Instruction* instruction, Frame* frame); 
    void invoke_monitorexit     (const ConstantPool* runtimeConstantPool, const Instruction* instruction, Frame* frame); 
    void invoke_wide            (const ConstantPool* runtimeConstantPool, const Instruction* instruction, Frame* frame); 
    void invoke_multianewarray  (const ConstantPool* runtimeConstantPool, const Instruction* instruction, Frame* frame); 
    void invoke_ifnull          (const ConstantPool* runtimeConstantPool, const Instruction* instruction, Frame* frame); 
    void invoke_ifnonnull       (const ConstantPool* runtimeConstantPool, const Instruction* instruction, Frame* frame); 
    void invoke_goto_w          (const ConstantPool* runtimeConstantPool, const Instruction* instruction, Frame* frame); 
    void invoke_jsr_w           (const ConstantPool* runtimeConstantPool, const Instruction* instruction, Frame* frame); 
    void invoke_breakpoint      (const ConstantPool* runtimeConstantPool, const Instruction* instruction, Frame* frame); 
    void invoke_impdep1         (const ConstantPool* runtimeConstantPool, const Instruction* instruction, Frame* frame); 
    void invoke_impdep2         (const ConstantPool* runtimeConstantPool, const Instruction* instruction, Frame* frame); 
}
#endif